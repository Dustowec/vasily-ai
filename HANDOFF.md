# VASILY AI — HANDOFF (передача контекста)
**Обновлено:** 2026-08-23
**Версия:** После завершения спринта 1 (перед рефакторингом памяти)
**Статус:** 🟡 Требуется рефакторинг памяти

---

## 1. ТЕКУЩЕЕ СОСТОЯНИЕ

**Ветка:** `develop` (чистая, запушена)
**Тесты:** 151 passed
**Покрытие:** ~65%
**Плагины:** 6
**Модули core:** 17

---

## 2. ЧТО РАБОТАЕТ (СТАБИЛЬНО)

| Компонент | Статус |
|-----------|--------|
| Логирование (structlog) | ✅ |
| Crash-репорты | ✅ |
| ReActLoop | ✅ |
| Плагины (все 6) | ✅ |
| Watchdog (мониторинг) | ✅ |
| Streamlit дашборд | ✅ |
| TokenManager | ✅ |
| HealthCheck | ✅ |
| GradientMemory (основные операции) | ⚠️ Работает, но требует уточнения логики |

---

## 3. ЧТО НЕ РАБОТАЕТ (КРИТИЧЕСКИЕ ПРОБЛЕМЫ)

### Проблема: `забудь всё` ведёт себя нестабильно
- **Текущее поведение:** TGS → HOT (штраф -20), HOT → COLD (штраф -10), но `score` не всегда обновляется, записи дублируются, контекст LLM не очищается полностью.
- **Ожидаемое поведение:** Пока не определено — требует обсуждения.

---

## 4. ЧТО ТРЕБУЕТ РЕШЕНИЯ (БЛОКИРУЮЩИЕ ВОПРОСЫ)

1. **Как должна работать команда `забудь всё`?**
   - Вариант А: Полная очистка всех зон (TGS, HOT, COLD) → `TGS=0, Hot=0, Cold=0`.
   - Вариант Б: Архивация TGS → HOT → COLD (как сейчас, но с правильной логикой).
   - Вариант В: Снятие защиты с TGS (перемещение в HOT), а COLD остаётся нетронутым.

2. **Как должна работать команда `забудь <тема>`?**
   - Удаление конкретной записи из любой зоны (с штрафом или без)?

3. **Как должна работать автоматическая архивация?**
   - Только через `compress_cycle` (по таймеру) или ещё как-то?

---

## 5. ПЛАН (ПРИНЯТ)

1. **Закоммитить текущее состояние** с пометкой «память сломана».
2. **Взять паузу** для переосмысления архитектуры памяти.
3. **Вернуться с новым планом** (ADR-011) и реализовать его.
4. **Протестировать** на реальных сценариях.

---

## 6. ИЗВЕСТНЫЕ ПРОБЛЕМЫ (НА МОМЕНТ ОСТАНОВКИ)

- `score` в HOT не уменьшается после `забудь всё да`.
- Дублирование записей (TGS + HOT) при загрузке из файлов.
- Контекст LLM не очищается полностью после ротации.
- Логика `forget_all` слишком сложная и запутанная.

---

## 7. СЛЕДУЮЩИЕ ШАГИ (ПОСЛЕ ПАУЗЫ)

1. Пересмотреть архитектуру памяти.
2. Написать ADR-011 с новым дизайном.
3. Реализовать простую, понятную логику:
   - `забудь всё` = полная очистка.
   - `забудь <тема>` = удаление конкретной записи.
   - Архивация — только через `compress_cycle`.
4. Написать тесты для всех сценариев.

---

## 8. КЛЮЧЕВЫЕ КОМАНДЫ

```powershell
# Проверить статус
git status

# Закоммитить текущее состояние
git add .
git commit -m "WIP: память сломана - требует рефакторинга"
git push

# Запустить тесты
pytest tests/ -q

# Проверить память
python debug_memory.py
