HANDOFF — Vasily AI
Обновлено: 2026-09-08, конец сессии ADR-013. Ветка: develop.

Статус
ADR-013 «Gradient Cascade Memory» — реализация ЗАВЕРШЕНА (§10.1–10.4).Тесты: 284 passed, 0 failed. ruff check . + pytest tests/ -v (БЕЗ --ignore).

Следующее ТЗ — собранные кандидаты (обсудить, потом специфицировать)
ЛЮБОЙ запрос пользователя ОБЯЗАН проходить через LLM.«Агент умный, а не быстрый» (решение Архитектора). Keyword-роутингweb_search в handle_request — «оптимизация» LLM-кодера, НЕ замысел.Ложное срабатывание подтверждено трассой: «у меня новости» (сообщение)уходит в веб-поиск вместо LLM. Побочный эффект: decay-тик в этой веткене тикает (ранний return до memory.decay) — нарушено «1 запрос = 1 тик».
forget по смыслу. «забудь кота» ищет ТОЧНЫЙ ключ → «не найдена»,ничего не удаляется (§8 так и задумано, но UX дырявый). Нужен механизм:найти кандидатов через поиск → показать юзеру → подтвердить → удалить.Вопрос амнистии user_fact (§8) при таком удалении — решить явно.
Семантика приватности forget. Точечный forget по ключу НЕ стирает,а высылает в COLD (40−50=−10 доживает ~80 тиков), и запись всё этовремя НАХОДИТСЯ поиском (§9 ищет все зоны). Если «забудь» = «удалинавсегда» — нужен мгновенный стирание или hidden-флаг для поиска.
Частичное редактирование памяти не существует. Кот в системномсаммари живёт, пока жива запись саммари целиком; «вырезать упоминание»нельзя. Осознать и либо принять, либо спроектировать.
Сделано в этой сессии
6.3: core/agent.py — cold_start_penalty() в initialize(); «забудь всё да»отвечает числами (удалено N / сохранено M / после тика X); decay привыключении убран. tests/test_agent.py обновлён (+2 регрессионных теста).
6.4a: tests/test_cascade_zones.py (14) — зоны, TGS, лестница промоции,кап 60, LRU, старт-−2, «один нагрев за тик», активный режим.
6.4b: tests/test_revival_lifecycle.py (16) — дистилляция/пере-валидация,траектория «разведка/прописка» §6, эмерджентный remember-вход из COLD,heat_facts-revival, поиск по summary.
6.4c: tests/test_user_fact_insurance.py (11) — амнистия 10 тиков,страховка поверх рестарта, ступенчатая ротация forget_all.
Легаси-тесты ADR-012 удалены (git rm); tests/conftest.py — границаlogging-хендлеров между тестами (шум «Logging error» убран).
Фиксы memory/manager.py, найденные тестами (тесты правы, код чинится):
двойное остывание мигранта (cold_start_penalty и decay одного тика);
ловушка ≤0.5 — от результата вычета, не от старого скора;
проверка снятия no_compress зовётся при каждой операции нагрева.ВАЖНО: само снятие — ТОЛЬКО score ≥ 8 И changed_since_revival.Перечитывание флаг НЕ снимает. Причина: бесконечная дистилляцияrevived-записей (кейс: COLD(−5) → recall 2.0 → очередь);
forget_all сжигал записи — ступени с hot_before/cold_before:удаляются только старожилы COLD, переселенцы доживают на ступень ниже.
Инварианты (нарушение = баг, залочено тестами)
Один нагрев записи за тик; повторный меняет только updated_at.
Удаление — только score < −50.0. Бессмертной памяти нет.
Событийная очередь — «впервые ОПУСТИВШАЯСЯ ≤ 5.0»; вошедшая в HOTс 2.0–4.8 в очередь не попадает и защищена no_compress.
forget_all: TGS → HOT(40) → COLD → удаление только старожилов COLD.
Хвосты на следующий раз
Удалить заглушку session_close() в memory/manager.py (вызывающих нет,проверено findstr) — при следующей правке этого файла.
Первый живой запуск агента применит старт-−2 и миграцию данных.Бэкап: data_backup_013/ в корне. Откат: xcopy data_backup_013 data /E /I /H.
data_backup_013 в git НЕ коммитить (при желании — в .gitignore).
Правила работы (важно для следующей сессии)
Заказчик не программист. Никаких диффов и точечных вставок: файл ЦЕЛИКОМна замену + пошаговые инструкции + готовые команды.
Файлы в чат не влезают: по одному за сообщение, при переполнении — частями.
После каждой выдачи кода — ruff + pytest; ожидаемое число тестов называтьзаранее. Красный прогон → коммит не делать.
Упавший тест = сначала подозрение на КОД; тест правится, только еслипроверяет устаревшее требование.
